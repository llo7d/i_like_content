Goal 1:
Add url in plugin & display the url in the plugin. ( )

1.1
Be able to console.log the chrome storage blocked urls ( )

Goal 2:
When that plugin is accessed, have a custom page that has a button "access" ( )